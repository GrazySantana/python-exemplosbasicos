Velocidade = int(input("Informe a velocidade: "))

# Uso do If ternário
(variable) alerta : Literal['Alta velocidade! Multado.']
alerta = "Alta velocidade! Multado." if velocidade >60 else "Dentro do limite de velocidade."

# Exibição
print(alerta)